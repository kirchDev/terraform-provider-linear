# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

# `go` is in here deliberately, unlike the sibling providers — this one handles
# a workspace-scoped API key, so it is worth scanning. The body switches Go to
# autobuild on its own; CodeQL rejects build-mode none for it.
name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main, dev]
    types: [opened, synchronize, reopened, ready_for_review]
  schedule:
    - cron: '34 23 * * 0'
  workflow_dispatch:

jobs:
  codeql:
    name: CodeQL
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read
    uses: kirchDev/workflows/.github/workflows/_codeql.yml@7fd4eaf529500fcd07c527ae70c839ab9ac7d76f # v0.7.0
    with:
      languages: '["actions", "go", "javascript-typescript"]'
