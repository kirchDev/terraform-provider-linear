# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

# The Terraform Registry reads this repo's tags, and GoReleaser uploads the
# signed artifacts onto the release release-please cuts.
name: Release Please

on:
  push:
    branches:
      - main

jobs:
  release-please:
    name: Release Please
    permissions:
      contents: write
      pull-requests: write
    uses: kirchDev/workflows/.github/workflows/_release-please.yml@f81bbe5fb5e8055b55a1ef5f861cc63a8e253622 # v0.1.3
    secrets:
      BWS_ACCESS_TOKEN: ${{ secrets.BWS_ACCESS_TOKEN }}

  goreleaser:
    name: GoReleaser
    needs: release-please
    if: needs.release-please.outputs.release-created == 'true'
    permissions:
      contents: write
    uses: kirchDev/workflows/.github/workflows/_publish-goreleaser.yml@f81bbe5fb5e8055b55a1ef5f861cc63a8e253622 # v0.1.3
    with:
      tag-name: ${{ needs.release-please.outputs.tag-name }}
    secrets:
      BWS_ACCESS_TOKEN: ${{ secrets.BWS_ACCESS_TOKEN }}
