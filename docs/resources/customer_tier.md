---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "linear_customer_tier Resource - linear"
subcategory: ""
description: |-
  Manages a customer tier in Linear — the band a customer is segmented into, which customer requests are weighed by.
  Requires Linear Customers to be enabled for the workspace; see customers_enabled on linear_workspace_settings. Tiers are workspace-wide, not per team.
---

# linear_customer_tier (Resource)

Manages a customer tier in Linear — the band a customer is segmented into, which customer requests are weighed by.

Requires Linear Customers to be enabled for the workspace; see `customers_enabled` on `linear_workspace_settings`. Tiers are workspace-wide, not per team.

## Example Usage

```terraform
resource "linear_customer_tier" "enterprise" {
  name         = "enterprise"
  display_name = "Enterprise"
  description  = "Contractual SLA, requests weighed highest"
  color        = "#eb5757"
  position     = 1
}

resource "linear_customer_tier" "free" {
  name         = "free"
  display_name = "Free"
  color        = "#bec2c8"
  position     = 3
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `color` (String) Colour of the tier indicator as a hex string, e.g. `#f2c94c`.

### Optional

- `description` (String) Description of what the tier represents.
- `display_name` (String) Name shown in the Linear UI. Set at least one of `name` and `display_name`.
- `name` (String) Internal name of the tier. Unique within the workspace. Set at least one of `name` and `display_name`.
- `position` (Number) Sort position within the workspace's tier ordering. Linear appends the tier at the end when unset.

### Read-Only

- `id` (String) UUID of the customer tier.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import by UUID.
terraform import linear_customer_tier.example a1b2c3d4-0000-0000-0000-000000000000
```
